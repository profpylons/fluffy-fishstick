{
  "name": "game-analytics-mcp",
  "version": "1.0.0",
  "description": "Game Analytics MCP Server with RAWG API integration",
  "scripts": {
    "dev:server": "wrangler dev server/src/index.ts",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.4",
    "hono": "^3.12.12",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251118.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.49.0"
  }
}
